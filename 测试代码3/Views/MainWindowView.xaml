<Window x:Class="CMTest2.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:local="clr-namespace:CMTest2"
        mc:Ignorable="d"
        Title="MainWindowView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="10,0,10,0"/>
                </Style>
            </StackPanel.Resources>
            <Button Width="80" Height="50" cal:Message.Attach="[Event Click] = [Action LogTest]"/>
            <Button Width="80" Height="50" Content="Start" cal:Message.Attach="[Event Click] = [Action StartTasks]"/>
        </StackPanel>
        <ListBox Grid.Row="1" Margin="10,10,10,10" Background="#F0F0F0" ItemsSource="{Binding Logs}" >

        </ListBox>

    </Grid>
</Window>
